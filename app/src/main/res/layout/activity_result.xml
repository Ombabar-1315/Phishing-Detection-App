<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_result.xml
    Result Screen — shows verdict badge, score, reasons, and action buttons.

    Location to save this file in Android Studio:
    app > res > layout > activity_result.xml
-->
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_light"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- ── Screen Title ── -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Scan Result"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="20dp"/>

        <!-- ── RISK BADGE (SAFE / SUSPICIOUS / PHISHING) ── -->
        <TextView
            android:id="@+id/tv_badge"
            android:layout_width="match_parent"
            android:layout_height="72dp"
            android:text="SCANNING..."
            android:textSize="26sp"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginBottom="16dp"/>

        <!-- ── Risk Score ── -->
        <TextView
            android:id="@+id/tv_score"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Risk Score: 0 / 100"
            android:textSize="16sp"
            android:textColor="@color/text_primary"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="8dp"/>

        <!-- ── Source type ── -->
        <TextView
            android:id="@+id/tv_source"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Detected via: Manual"
            android:textSize="13sp"
            android:textColor="@color/text_secondary"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="20dp"/>

        <!-- ── URL Card ── -->
        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            app:cardCornerRadius="10dp"
            app:cardElevation="1dp"
            xmlns:app="http://schemas.android.com/apk/res-auto">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="14dp">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="URL SCANNED"
                    android:textSize="11sp"
                    android:textColor="@color/text_secondary"
                    android:letterSpacing="0.1"
                    android:layout_marginBottom="6dp"/>

                <TextView
                    android:id="@+id/tv_url"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="https://example.com"
                    android:textSize="13sp"
                    android:textColor="@color/text_primary"
                    android:lineSpacingExtra="3dp"/>
            </LinearLayout>
        </androidx.cardview.widget.CardView>

        <!-- ── Reasons Card ── -->
        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            app:cardCornerRadius="10dp"
            app:cardElevation="1dp"
            xmlns:app="http://schemas.android.com/apk/res-auto">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="14dp">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="WHY THIS RESULT"
                    android:textSize="11sp"
                    android:textColor="@color/text_secondary"
                    android:letterSpacing="0.1"
                    android:layout_marginBottom="10dp"/>

                <!-- Reasons are added here dynamically by ResultActivity.java -->
                <LinearLayout
                    android:id="@+id/ll_reasons"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"/>

            </LinearLayout>
        </androidx.cardview.widget.CardView>

        <!-- ── Action Buttons ── -->

        <!-- Back to Home -->
        <Button
            android:id="@+id/btn_back"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:text="Scan Another URL"
            android:textSize="15sp"
            android:textColor="@color/white"
            android:backgroundTint="@color/primary_blue"
            android:layout_marginBottom="10dp"/>

        <!-- View History -->
        <Button
            android:id="@+id/btn_history"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:text="View History"
            android:textSize="15sp"
            android:textColor="@color/primary_blue"
            android:backgroundTint="@android:color/transparent"
            android:layout_marginBottom="10dp"/>

        <!-- Report -->
        <Button
            android:id="@+id/btn_report"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:text="Report This URL"
            android:textSize="15sp"
            android:textColor="@color/phishing_red"
            android:backgroundTint="@android:color/transparent"
            android:layout_marginBottom="16dp"/>

    </LinearLayout>
</ScrollView>